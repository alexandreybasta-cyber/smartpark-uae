# Credentials & secrets
push-via-api.sh
Secrets.xcconfig

# Qoder/Claude
.qoder/
.claude/

# Dependencies
node_modules/
venv/
.venv/

# Build outputs
.next/
build/
dist/
out/

# Environment
.env
.env.local
.env.*.local

# IDE & OS
.DS_Store
.vscode/
.idea/
*.swp
xcuserdata/
*.xcuserstate

# Generated thumbnails & build scripts
spotsense-thumbnail*.png
build_thumb.py
upscale_appstore.py
upscale_appstore_screenshots.sh

# Caches
.fontcache/
.expo/

# Database
*.db
*.sqlite
*.db-shm
*.db-wal

# Archives
*.zip

# Video output
remotion-video/out/
hyperframes/spotsense-demos/output/full-reel.mp4
push-via-api.sh
.qoder/
.claude/

Website/spotsense-deploy.zip
hyperframes/spotsense-demos/output/full-reel.mp4

# Local-only business/launch artifacts (NOT part of the SpotSense code repo)
.business-plan-build/
SpotSense_Business_Plan_2026.docx
UAE-Launch-Kit/
vibe_images/

# Runtime media uploads (vision pipeline) — never commit operator uploads
backend/uploads/

# Auto-collected labelled training data (frames + labels.jsonl) for later YOLO
# fine-tuning — local-only, never push to the SpotSense repo.
backend/vision/dataset/

# Fine-tune runner: torch venv + run artifacts (status/log/scratch).  The
# trained bays.onnx IS committed (prod can't train, it only infers).
backend/train-venv/
backend/vision/train_status.json
backend/vision/train.log
backend/vision/trainwork/

# Local test-image scratch folder (not part of the repo)
VIsual camera bay detection/
